<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myappli">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyAppli">
        <activity
            android:name=".screen.main.MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/Theme.MyAppli.NoActionBar">

        </activity>
        <activity android:name=".screen.splash.SplashActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".screen.subCategory.SubCategoryActivity" />
        <activity android:name=".screen.tag.TagActivity" />
        <activity android:name=".screen.category.CategoryActivity">
        </activity>
    </application>

</manifest>